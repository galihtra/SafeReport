import 'package:flutter/material.dart';

class PendampinganNotif extends StatelessWidget {
  const PendampinganNotif({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Notif Pendampingan"),
      ),
    );
  }
}
