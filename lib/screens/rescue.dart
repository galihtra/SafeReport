import 'package:flutter/material.dart';

class Rescue extends StatelessWidget {
  const Rescue({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Rescue"),),
    );
  }
}