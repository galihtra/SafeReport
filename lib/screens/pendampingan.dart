import 'package:flutter/material.dart';

class Pendampingan extends StatelessWidget {
  const Pendampingan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Pendampingan")),
    );
  }
}