import 'package:flutter/material.dart';

class AdminEdukasi extends StatelessWidget {
  const AdminEdukasi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Admin Edukasi"),
      ),
    );
  }
}
