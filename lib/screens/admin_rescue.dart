import 'package:flutter/material.dart';

class AdminRescue extends StatelessWidget {
  const AdminRescue({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Admin Rescue"),
      ),
    );
  }
}
